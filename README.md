### Requirements
 - Go 1.18
 - MongoDB
 - RabbitMQ


#### Start application
```
docker-compose build
docker-compose up
```
And then open: http://localhost:8089/

#### Tips
To start local build app:

```
go build -o web-go
```
